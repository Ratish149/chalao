from django.urls import path
# from .views import ReviewListCreateView, ReviewRetrieveUpdateDestroyAPIView

urlpatterns = [
    # path('reviews/', ReviewListCreateView.as_view(), name='review-list-create'),
    # path('reviews/<str:review_id>/', ReviewRetrieveUpdateDestroyAPIView.as_view(), name='review-detail'),
]